# Build stage
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .

# Production stage
FROM node:20-alpine
WORKDIR /app
COPY --from=builder /app .
RUN mkdir -p uploads
EXPOSE 3000
CMD ["node", "server.js"]
